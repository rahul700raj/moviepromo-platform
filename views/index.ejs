<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MoviePromo - Promote Your Movies</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
  <%- include('partials/navbar') %>
  
  <section class="hero">
    <div class="hero-content">
      <h1>🎬 Promote Your Movies to Millions</h1>
      <p>Upload your story, reach audiences, and grow your fanbase with digital & organic marketing</p>
      <div class="hero-buttons">
        <% if (!user) { %>
          <a href="/register" class="btn btn-primary">Get Started - 3 Free Credits</a>
          <a href="/login" class="btn btn-secondary">Login</a>
        <% } else { %>
          <a href="/upload" class="btn btn-primary">Upload Your Movie</a>
          <a href="/dashboard" class="btn btn-secondary">Dashboard</a>
        <% } %>
      </div>
    </div>
  </section>

  <section class="features">
    <h2>Why Choose MoviePromo?</h2>
    <div class="features-grid">
      <div class="feature-card">
        <i class="fas fa-upload"></i>
        <h3>Easy Upload</h3>
        <p>Upload your movie story, poster, and trailer in minutes</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-bullhorn"></i>
        <h3>Digital Marketing</h3>
        <p>Automated social media promotion and targeted ads</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-chart-line"></i>
        <h3>Organic Growth</h3>
        <p>SEO optimization and community engagement</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-users"></i>
        <h3>Reach Audience</h3>
        <p>Connect with millions of movie enthusiasts</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-analytics"></i>
        <h3>Analytics</h3>
        <p>Track views, engagement, and audience insights</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-credit-card"></i>
        <h3>Flexible Pricing</h3>
        <p>3 free credits, then affordable auto-pay plans</p>
      </div>
    </div>
  </section>

  <% if (movies.length > 0) { %>
  <section class="featured-movies">
    <h2>Featured Movies</h2>
    <div class="movies-grid">
      <% movies.forEach(movie => { %>
        <div class="movie-card">
          <img src="<%= movie.poster || '/images/placeholder.jpg' %>" alt="<%= movie.title %>">
          <div class="movie-info">
            <h3><%= movie.title %></h3>
            <p><%= movie.genre %></p>
            <div class="movie-stats">
              <span><i class="fas fa-eye"></i> <%= movie.views %></span>
              <span><i class="fas fa-heart"></i> <%= movie.likes %></span>
            </div>
            <a href="/movie/<%= movie.id %>" class="btn btn-small">View Details</a>
          </div>
        </div>
      <% }); %>
    </div>
  </section>
  <% } %>

  <%- include('partials/footer') %>
</body>
</html>